import styles from './DocsView.module.css'

export default function DocsView() {
  return (
    <section className={styles.docs}>
      <h1>Docs</h1>
      <p>everything you need to actually run the training environment.</p>
    </section>
  )
}
